{
  "name": "onliffe",
  "version": "0.1.0",
  "private": true,
  "description": "Online word processor built with Rust, WebAssembly, React and Server.js",
  "scripts": {
    "pretest": "lerna exec -- xo --ignore dist.js",
    "test": "lerna run test",
    "lint": "npm run pretest",
    "build": "lerna run build",
    "fix": "lerna exec -- xo --prettier --fix --ignore dist.js",
    "prep": "lerna bootstrap"
  },
  "author": "thatlittlegit",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "lerna": "^2.7.1",
    "xo": "^0.18.2"
  }
}
